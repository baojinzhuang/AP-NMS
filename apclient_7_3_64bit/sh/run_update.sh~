#!/bin/sh

echo "执行的文件名：$0";
echo "传入文件：$1";

tftp 
if [ $? -ne 0 ]; then
    echo "update failed"
else
    echo "update succeed"
fi

cd ~/Downloads

if [ $? -ne 0 ]; then
    echo "cd failed"
else
    echo "cd succeed"
fi
